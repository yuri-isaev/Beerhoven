<resources>
    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.MaterialComponents">
        <item name="android:textColor">@color/colorMainGrey</item>
        <item name="android:textSize">@dimen/text_size</item>
        <item name="android:windowFullscreen">true</item>
        <item name="colorPrimary">@color/colorMainBlack</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.Neumorph.Button" parent="Widget.Neumorph.Button">
        <item name="android:textColor">@color/colorPrimaryDark</item>
        <item name="android:textSize">@dimen/text_22</item>
        <item name="fontFamily">@font/catorze27_style1_bold</item>
        <item name="neumorph_backgroundColor">@color/colorAccent</item>
        <item name="neumorph_shadowColorDark">@color/colorShadowDark</item>
        <item name="neumorph_shadowColorLight">@color/colorMainWhite</item>
    </style>
</resources>